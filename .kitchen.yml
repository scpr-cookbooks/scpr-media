---
driver:
  name: vagrant

provisioner:
  name: chef_zero

platforms:
  - name: ubuntu-12.04

suites:
  - name: default
    run_list:
      - recipe[scpr-media::default]
    attributes:
      scpr_tools:
        media_nfs_ip: 10.100.0.12
